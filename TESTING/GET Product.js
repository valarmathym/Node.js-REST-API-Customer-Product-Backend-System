/*
o test the GET request in your Node.js REST API, you can use a tool like 
Postman or curl command in your terminal.
Here's an example of how to use curl to make a GET request to your server:
*/
curl http://localhost:3000/products

/*
This sends a GET request to http://localhost:3000/products.

If the GET request is successful, you should see a JSON response containing 
an array of all the products in the database.

Alternatively, you can use Postman to test your GET request by following these 
steps:

Open Postman and create a new request.
Set the request method to GET and the URL to http://localhost:3000/products.
Click the "Send" button to send the GET request.
If the GET request is successful, you should see a JSON response containing 
an array of all the products in the database.
*/




